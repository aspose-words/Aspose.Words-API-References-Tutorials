---
title: По диапазону страниц
linktitle: По диапазону страниц
second_title: Справочник по API Aspose.Words для .NET
description: Простое извлечение по диапазону страниц из документа Word с помощью Aspose.Words для .NET. Пошаговое руководство.
type: docs
weight: 10
url: /ru/net/split-document/by-page-range/
---

## Введение
В этом руководстве мы шаг за шагом проведем вас, чтобы понять и использовать функцию «По диапазону страниц» Aspose.Words для .NET. Эта функция позволяет извлечь определенную часть большого документа Word, используя заданный диапазон страниц. Мы предоставим вам полный исходный код и выходные форматы Markdown, чтобы вам было легче понять и использовать его позже.

## Требования
Прежде чем начать, убедитесь, что у вас есть следующее:

1. Aspose.Words for .NET установлен на вашем компьютере для разработки.
2. Большой файл Word, из которого вы хотите извлечь определенную часть.

Теперь, когда мы рассмотрели требования, мы можем перейти к действиям по использованию функции «По диапазону страниц».

## Шаг 1: Инициализация и загрузка документа
После того, как вы настроили среду разработки, вам нужно инициализировать и загрузить документ Word, из которого вы хотите извлечь определенную часть. Вот код для использования:

```csharp
// Путь к каталогу документов.
string dataDir = "YOUR_DIRECTORY_OF_DOCUMENTS";
Document doc = new Document(dataDir + "Name_of_large_document.docx");
```

Обязательно замените «YOUR_DOCUMENTS_DIRECTORY» на фактический путь к каталогу ваших документов, а «Name_of_large_document.docx» — на имя вашего большого файла Word.

## Шаг 2: Извлечение части документа
 Теперь, когда мы загрузили документ, мы можем извлечь определенную часть, используя`ExtractPages` с нужным диапазоном страниц. Вот как это сделать:

```csharp
Document extractedPages = doc.ExtractPages(3, 6);
```

В этом примере мы извлекаем страницы 3-6 из исходного документа. Вы можете настроить номера страниц в соответствии с вашими потребностями.

## Шаг 3: Сохраните извлеченную часть
Как только мы извлекли нужные страницы, мы можем сохранить их в новом документе Word. Вот как:

```csharp
extractedPages.Save(dataDir + "Document_Extraits.ParRangeDePages.docx");
```

Обязательно замените «Document_Extraits.ParPlageDePages.docx» на желаемое имя выходного файла.

### Пример исходного кода для диапазона страниц с использованием Aspose.Words для .NET

```csharp
// Путь к каталогу документов.
string dataDir = "YOUR DOCUMENT DIRECTORY";
Document doc = new Document(MyDir + "Big document.docx");

// Получить часть документа.
Document extractedPages = doc.ExtractPages(3, 6);
extractedPages.Save(dataDir + "SplitDocument.ByPageRange.docx");
```

## Заключение
Поздравляем! Вы узнали, как использовать «По диапазону страниц» из Aspose.Words для .NET. Теперь вы можете легко извлекать определенные части большого документа Word, используя заданный диапазон страниц. Не стесняйтесь экспериментировать с другими мощными функциями Aspose. .Words для удовлетворения ваших конкретных потребностей.

