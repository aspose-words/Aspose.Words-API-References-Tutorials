---
title: एन्क्रिप्टेड वर्ड दस्तावेज़ सत्यापित करें
linktitle: एन्क्रिप्टेड वर्ड दस्तावेज़ सत्यापित करें
second_title: Aspose.Words दस्तावेज़ प्रसंस्करण API
description: एक वर्ड दस्तावेज़ को .NET के लिए Aspose.Words के साथ एन्क्रिप्टेड किया गया है, यह सत्यापित करने के लिए चरण-दर-चरण मार्गदर्शिका।
type: docs
weight: 10
url: /hi/net/programming-with-fileformat/verify-encrypted-document/
---

यह लेख .NET के लिए Aspose.Words के साथ एन्क्रिप्टेड वर्ड दस्तावेज़ सत्यापन सुविधा का उपयोग करने के तरीके पर चरण-दर-चरण मार्गदर्शिका प्रदान करता है। हम कोड के प्रत्येक भाग को विस्तार से समझाएंगे। इस ट्यूटोरियल के अंत में, आप यह समझने में सक्षम होंगे कि कोई दस्तावेज़ एन्क्रिप्टेड है या नहीं।

शुरू करने से पहले, सुनिश्चित करें कि आपने अपने प्रोजेक्ट में Aspose.Words for .NET लाइब्रेरी को इंस्टॉल और कॉन्फ़िगर किया है। आप Aspose वेबसाइट पर लाइब्रेरी और इंस्टॉलेशन निर्देश पा सकते हैं।

## चरण 1: दस्तावेज़ निर्देशिका निर्धारित करें

 शुरू करने के लिए, आपको उस निर्देशिका का पथ परिभाषित करना होगा जहाँ आपके दस्तावेज़ स्थित हैं।`"YOUR DOCUMENT DIRECTORY"` आपके दस्तावेज़ निर्देशिका के वास्तविक पथ के साथ.

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
```

## चरण 2: फ़ाइल स्वरूप का पता लगाएं

 इसके बाद, हम उपयोग करते हैं`DetectFileFormat` की विधि`FileFormatUtil` फ़ाइल प्रारूप जानकारी का पता लगाने के लिए क्लास। इस उदाहरण में, हम मानते हैं कि एन्क्रिप्टेड दस्तावेज़ को "Encrypted.docx" कहा जाता है और यह निर्दिष्ट दस्तावेज़ निर्देशिका में स्थित है।

```csharp
FileFormatInfo info = FileFormatUtil.DetectFileFormat(dataDir + "Encrypted.docx");
```

## चरण 3: जाँचें कि दस्तावेज़ एन्क्रिप्टेड है या नहीं

 हम उपयोग करते हैं`IsEncrypted` की संपत्ति`FileFormatInfo`यह जाँचने के लिए कि क्या दस्तावेज़ एन्क्रिप्टेड है या नहीं। यह प्रॉपर्टी रिटर्न करती है`true` यदि दस्तावेज़ एन्क्रिप्टेड है, अन्यथा यह वापस आ जाता है`false`हम परिणाम को कंसोल में प्रदर्शित करते हैं।

```csharp
Console.WriteLine(info.IsEncrypted);
```

बस इतना ही ! आपने सफलतापूर्वक जाँच कर ली है कि कोई दस्तावेज़ Aspose.Words for .NET का उपयोग करके एन्क्रिप्टेड है या नहीं।

### .NET के लिए Aspose.Words के साथ एन्क्रिप्टेड दस्तावेज़ों को सत्यापित करने के लिए उदाहरण स्रोत कोड

```csharp

	// दस्तावेज़ निर्देशिका का पथ.
	string dataDir = "YOUR DOCUMENT DIRECTORY";
	FileFormatInfo info = FileFormatUtil.DetectFileFormat(dataDir + "Encrypted.docx");
	Console.WriteLine(info.IsEncrypted);
            
        
```

## अक्सर पूछे जाने वाले प्रश्नों

### प्रश्न: एन्क्रिप्टेड वर्ड दस्तावेज़ को सत्यापित करने के चरण क्या हैं?

एन्क्रिप्टेड वर्ड दस्तावेज़ को सत्यापित करने के चरण इस प्रकार हैं:

दस्तावेज़ निर्देशिका को परिभाषित करें.

फ़ाइल स्वरूप का पता लगाएं.

जाँचें कि दस्तावेज़ एन्क्रिप्टेड है या नहीं.

### प्रश्न: मैं दस्तावेज़ निर्देशिका कैसे सेट कर सकता हूँ?
 दस्तावेज़ निर्देशिका सेट करने के लिए, आपको प्रतिस्थापित करने की आवश्यकता है`"YOUR DOCUMENT DIRECTORY"` निम्नलिखित कोड में अपने दस्तावेज़ निर्देशिका का वास्तविक पथ लिखें:

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
```

### प्रश्न: फ़ाइल स्वरूप का पता कैसे लगाएं?
 आप इसका उपयोग कर सकते हैं`DetectFileFormat` की विधि`FileFormatUtil`फ़ाइल प्रारूप जानकारी का पता लगाने के लिए क्लास। निम्नलिखित उदाहरण में, हम मानते हैं कि एन्क्रिप्टेड दस्तावेज़ को "Encrypted.docx" कहा जाता है और यह निर्दिष्ट दस्तावेज़ निर्देशिका में स्थित है:

```csharp
FileFormatInfo info = FileFormatUtil.DetectFileFormat(dataDir + "Encrypted.docx");
```

### प्रश्न: कैसे जांचें कि दस्तावेज़ एन्क्रिप्टेड है या नहीं?
 आप इसका उपयोग कर सकते हैं`IsEncrypted` की संपत्ति`FileFormatInfo`यह जाँचने के लिए कि क्या दस्तावेज़ एन्क्रिप्टेड है या नहीं। यह प्रॉपर्टी रिटर्न करती है`true` यदि दस्तावेज़ एन्क्रिप्टेड है, अन्यथा यह वापस आ जाता है`false`परिणाम कंसोल में प्रदर्शित होता है:

```csharp
Console.WriteLine(info.IsEncrypted);
```

### प्रश्न: Aspose.Words for .NET का उपयोग करके यह कैसे जांचें कि कोई दस्तावेज़ एन्क्रिप्टेड है या नहीं?
इस ट्यूटोरियल में बताए गए चरणों का पालन करके और दिए गए स्रोत कोड को चलाकर, आप जांच सकते हैं कि कोई दस्तावेज़ .NET के लिए Aspose.Words का उपयोग करके एन्क्रिप्ट किया गया है या नहीं।
