---
title: แปลงเอกสาร Word เป็น PDF 1.7
linktitle: แปลงเอกสาร Word เป็น PDF 1.7
second_title: Aspose.Words API การประมวลผลเอกสาร
description: เรียนรู้วิธีแปลงเอกสาร word เป็น PDF 1.7 ด้วย Aspose.Words สำหรับ .NET
type: docs
weight: 10
url: /th/net/programming-with-pdfsaveoptions/conversion-to-pdf-17/
---

ในบทช่วยสอนนี้ เราจะอธิบายขั้นตอนต่างๆ ในการแปลงเอกสารคำเป็น PDF 1.7 ด้วย Aspose.Words สำหรับ .NET การแปลงเป็น PDF 1.7 ช่วยให้คุณสร้างไฟล์ PDF ที่เป็นไปตามมาตรฐาน PDF 1.7 ทำตามขั้นตอนด้านล่าง:

## ขั้นตอนที่ 1: กำลังโหลดเอกสาร

เริ่มต้นด้วยการอัปโหลดเอกสารที่คุณต้องการแปลงเป็น PDF:

```csharp
string dataDir = "YOUR DOCUMENTS DIRECTORY";
Document doc = new Document(dataDir + "Rendering.docx");
```

อย่าลืมระบุเส้นทางที่ถูกต้องไปยังเอกสารของคุณ

## ขั้นตอนที่ 2: ตั้งค่าตัวเลือกการแปลง PDF

สร้างอินสแตนซ์ของคลาส PdfSaveOptions และระบุเวอร์ชันของมาตรฐาน PDF ที่คุณต้องการใช้:

```csharp
PdfSaveOptions saveOptions = new PdfSaveOptions { Compliance = PdfCompliance.Pdf17 };
```

ตัวเลือกนี้ช่วยให้แน่ใจว่าไฟล์ PDF ที่สร้างขึ้นเป็นไปตามมาตรฐาน PDF 1.7

## ขั้นตอนที่ 3: แปลงเอกสารเป็น PDF

 ใช้`Save` วิธีการแปลงเอกสารเป็น PDF โดยระบุตัวเลือกการแปลง:

```csharp
doc.Save(dataDir + "WorkingWithPdfSaveOptions.ConversionToPdf17.pdf", saveOptions);
```

ตรวจสอบให้แน่ใจว่าได้ระบุเส้นทางที่ถูกต้องเพื่อบันทึก PDF ที่แปลงแล้ว

### ตัวอย่างซอร์สโค้ดสำหรับการแปลงเป็น Pdf 17 โดยใช้ Aspose.Words สำหรับ .NET

นี่คือซอร์สโค้ดที่สมบูรณ์สำหรับการแปลงเป็น PDF 1.7 ด้วย Aspose.Words สำหรับ .NET:

```csharp

	// เส้นทางไปยังไดเร็กทอรีเอกสาร
	string dataDir = "YOUR DOCUMENT DIRECTORY";
	Document doc = new Document(dataDir + "Rendering.docx");

	PdfSaveOptions saveOptions = new PdfSaveOptions { Compliance = PdfCompliance.Pdf17 };

	doc.Save(dataDir + "WorkingWithPdfSaveOptions.ConversionToPdf17.pdf", saveOptions);

```

เมื่อทำตามขั้นตอนเหล่านี้ คุณจะสามารถแปลงเป็น PDF 1.7 ด้วย Aspose.Words for .NET ได้อย่างง่ายดาย


## บทสรุป

ในบทช่วยสอนนี้ เราได้อธิบายวิธีการแปลงเอกสาร Word เป็น PDF 1.7 โดยใช้ Aspose.Words สำหรับ .NET ด้วยการทำตามขั้นตอนที่อธิบายไว้ คุณสามารถสร้างไฟล์ PDF ที่สอดคล้องกับมาตรฐาน PDF 1.7 ได้อย่างง่ายดาย อย่าลืมระบุเส้นทางที่ถูกต้องไปยังเอกสาร Word ของคุณและกำหนดค่าตัวเลือกสำหรับการแปลงเป็น PDF ตามต้องการ การแปลงเป็น PDF 1.7 ช่วยให้มั่นใจได้ถึงความเข้ากันได้สูงสุดและความสามารถในการอ่านบนแพลตฟอร์มต่างๆ

### คำถามที่พบบ่อย

#### ถาม: การแปลง Word เป็น PDF 1.7 คืออะไร
ตอบ: การแปลงเอกสาร Word เป็น PDF 1.7 คือการสร้างไฟล์ PDF ที่เป็นไปตามมาตรฐาน PDF 1.7 มาตรฐานนี้ระบุคุณสมบัติและข้อกำหนดสำหรับไฟล์ PDF ช่วยให้สามารถใช้งานร่วมกันได้อย่างเหมาะสมและสามารถอ่านได้บนแพลตฟอร์มต่างๆ

#### ถาม: ฉันจะแปลงเอกสาร Word เป็น PDF 1.7 โดยใช้ Aspose.Words สำหรับ .NET ได้อย่างไร
ตอบ: หากต้องการแปลงเอกสาร Word เป็น PDF 1.7 โดยใช้ Aspose.Words สำหรับ .NET ให้ทำตามขั้นตอนเหล่านี้:

 กำหนดเส้นทางไดเร็กทอรีที่มีเอกสารของคุณอยู่โดยการแทนที่`"YOUR DOCUMENTS DIRECTORY"` ด้วยเส้นทางจริงของไดเร็กทอรีเอกสารของคุณ

 โหลดเอกสาร Word ที่คุณต้องการแปลงเป็น PDF โดยใช้`Document` และระบุเส้นทางไปยังเอกสาร Word ในไดเร็กทอรีเอกสารที่ระบุ

 กำหนดค่าการแปลงเป็นตัวเลือก PDF โดยการสร้างอินสแตนซ์ของ`PdfSaveOptions`และระบุเวอร์ชันของมาตรฐาน PDF ที่คุณต้องการใช้โดยใช้`Compliance` ทรัพย์สินที่มีมูลค่า`PdfCompliance. Pdf17` เพื่อสร้างไฟล์ PDF ที่เป็นไปตามมาตรฐาน PDF 1.7

 บันทึกเอกสารในรูปแบบ PDF โดยใช้ไฟล์`Save` วิธีการของ`Document` คลาสที่ระบุเส้นทางและตัวเลือกการบันทึก

#### ถาม: การแปลงเป็น PDF 1.7 ด้วย Aspose.Words สำหรับ .NET มีประโยชน์อย่างไร
ตอบ: ข้อดีของการแปลงเป็น PDF 1.7 ด้วย Aspose.Words สำหรับ .NET คือ:

รองรับ PDF 1.7: การแปลงเป็น PDF 1.7 ช่วยให้มั่นใจได้ว่าไฟล์ PDF ที่สร้างขึ้นนั้นเป็นไปตามมาตรฐาน PDF 1.7 ทำให้มั่นใจได้ถึงความเข้ากันได้และความสามารถในการอ่านบนแพลตฟอร์มที่แตกต่างกัน

การเก็บรักษาการจัดรูปแบบเอกสาร: Aspose.Words สำหรับ .NET ช่วยให้มั่นใจในการแปลงเอกสาร Word ได้อย่างแม่นยำโดยการรักษาการจัดรูปแบบ รูปภาพ และสไตล์ ทำให้ได้ไฟล์ PDF ที่เป็นต้นฉบับอย่างแท้จริง