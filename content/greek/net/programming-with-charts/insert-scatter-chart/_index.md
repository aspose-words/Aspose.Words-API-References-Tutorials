---
title: Εισαγωγή γραφήματος διασποράς στο έγγραφο του Word
linktitle: Εισαγωγή γραφήματος διασποράς στο έγγραφο του Word
second_title: Aspose.Words Document Processing API
description: Μάθετε πώς να εισάγετε ένα γράφημα scatter σε ένα έγγραφο χρησιμοποιώντας το Aspose.Words για .NET. Προσθέστε δεδομένα σειράς με συντεταγμένες X και Y.
type: docs
weight: 10
url: /el/net/programming-with-charts/insert-scatter-chart/
---

Αυτό το σεμινάριο εξηγεί πώς να χρησιμοποιήσετε το Aspose.Words για .NET για την εισαγωγή ενός γραφήματος scatter σε ένα έγγραφο. Ο παρεχόμενος πηγαίος κώδικας δείχνει πώς να δημιουργήσετε ένα γράφημα, να προσθέσετε δεδομένα σειράς και να αποθηκεύσετε το έγγραφο.

## Βήμα 1: Ρύθμιση του έργου

Βεβαιωθείτε ότι έχετε τις ακόλουθες προϋποθέσεις:

- Εγκαταστάθηκε η βιβλιοθήκη Aspose.Words για .NET. Μπορείτε να το κατεβάσετε χρησιμοποιώντας τη Διαχείριση πακέτων NuGet για να το εγκαταστήσετε.
- Μια διαδρομή καταλόγου εγγράφων όπου θα αποθηκευτεί το έγγραφο εξόδου.

## Βήμα 2: Δημιουργήστε ένα νέο έγγραφο και εισαγάγετε ένα γράφημα

 Δημιούργησε ένα νέο`Document` αντικείμενο και α`DocumentBuilder` για τη δημιουργία του εγγράφου.

```csharp
// Διαδρομή στον κατάλογο εγγράφων σας
string dataDir = "YOUR DOCUMENT DIRECTORY";

Document doc = new Document();
DocumentBuilder builder = new DocumentBuilder(doc);
```

 Στη συνέχεια, χρησιμοποιήστε το`InsertChart` μέθοδος του`DocumentBuilder` για να εισαγάγετε ένα διάγραμμα διασποράς στο έγγραφο.

```csharp
Shape shape = builder.InsertChart(ChartType.Scatter, 432, 252);
Chart chart = shape.Chart;
```

## Βήμα 3: Προσθέστε δεδομένα σειράς στο γράφημα

Προσθέστε δεδομένα σειράς στο γράφημα. Σε αυτό το παράδειγμα, θα προσθέσουμε δύο σύνολα συντεταγμένων X και Y.

```csharp
chart.Series.Add("Aspose Series 1", new double[] { 0.7, 1.8, 2.6 }, new double[] { 2.7, 3.2, 0.8 });
```

## Βήμα 4: Αποθηκεύστε το έγγραφο

 Τέλος, αποθηκεύστε το έγγραφο στον καθορισμένο κατάλογο χρησιμοποιώντας το`Save` μέθοδος του`Document` αντικείμενο.

```csharp
doc.Save(dataDir + "WorkingWithCharts.InsertScatterChart.docx");
```

Αυτό ολοκληρώνει την υλοποίηση της εισαγωγής ενός γραφήματος scatter χρησιμοποιώντας το Aspose.Words για .NET.

### Παράδειγμα πηγαίου κώδικα για Εισαγωγή γραφήματος Scatter χρησιμοποιώντας Aspose.Words για .NET 

```csharp
	// Διαδρομή στον κατάλογο εγγράφων σας
	string dataDir = "YOUR DOCUMENT DIRECTORY";

	Document doc = new Document();
	DocumentBuilder builder = new DocumentBuilder(doc);
	Shape shape = builder.InsertChart(ChartType.Scatter, 432, 252);
	Chart chart = shape.Chart;
	chart.Series.Add("Aspose Series 1", new double[] { 0.7, 1.8, 2.6 }, new double[] { 2.7, 3.2, 0.8 });
	doc.Save(dataDir + "WorkingWithCharts.InsertScatterChart.docx");
```

## συμπέρασμα

Σε αυτό το σεμινάριο, μάθατε πώς να εισάγετε ένα γράφημα scatter σε ένα έγγραφο του Word χρησιμοποιώντας το Aspose.Words για .NET. Ακολουθώντας τον οδηγό βήμα προς βήμα και χρησιμοποιώντας τον παρεχόμενο πηγαίο κώδικα, μπορείτε να δημιουργήσετε ένα νέο έγγραφο, να εισαγάγετε ένα διάγραμμα διασποράς, να προσθέσετε δεδομένα σειρών με συντεταγμένες X και Y και να αποθηκεύσετε το έγγραφο με το γράφημα.

Το Aspose.Words for .NET παρέχει ένα ολοκληρωμένο API για επεξεργασία λέξεων με γραφήματα σε έγγραφα του Word. Τα διαγράμματα διασποράς είναι χρήσιμα για την οπτικοποίηση και την ανάλυση δεδομένων με δύο αριθμητικές μεταβλητές. Με το Aspose.Words για .NET, μπορείτε εύκολα να δημιουργήσετε γραφήματα scatter που αντιπροσωπεύουν τη σχέση μεταξύ των τιμών X και Y και προσδιορίζουν μοτίβα ή τάσεις στα δεδομένα.

Χρησιμοποιώντας το Aspose.Words για .NET, μπορείτε να αυτοματοποιήσετε τη διαδικασία δημιουργίας εγγράφων με γραφήματα scatter, εξοικονομώντας χρόνο και προσπάθεια στη μη αυτόματη δημιουργία εγγράφων. Η βιβλιοθήκη προσφέρει ένα ευρύ φάσμα τύπων γραφημάτων, συμπεριλαμβανομένων γραφημάτων scatter, και παρέχει διάφορες επιλογές προσαρμογής για να προσαρμόσετε την εμφάνιση του γραφήματος σύμφωνα με τις ανάγκες σας.

### Συχνές ερωτήσεις

#### Q1. Τι είναι ένα διάγραμμα διασποράς;
Το διάγραμμα διασποράς είναι ένας τύπος γραφήματος που εμφανίζει τη σχέση μεταξύ δύο αριθμητικών μεταβλητών. Αποτελείται από μια σειρά σημείων που απεικονίζονται σε ένα πλέγμα συντεταγμένων, με τη μία μεταβλητή να αναπαρίσταται στον άξονα Χ και την άλλη μεταβλητή στον άξονα Υ. Τα διαγράμματα διασποράς χρησιμοποιούνται για τον προσδιορισμό προτύπων, συσχετίσεων ή τάσεων μεταξύ δύο συνόλων σημείων δεδομένων.

#### Ε2. Μπορώ να προσθέσω πολλές σειρές στο διάγραμμα διασποράς;
Ναι, μπορείτε να προσθέσετε πολλές σειρές στο διάγραμμα διασποράς χρησιμοποιώντας το Aspose.Words για .NET. Κάθε σειρά αντιπροσωπεύει ένα σύνολο σημείων δεδομένων με τις αντίστοιχες συντεταγμένες X και Y. Προσθέτοντας πολλές σειρές, μπορείτε να συγκρίνετε και να αναλύσετε διαφορετικά σύνολα δεδομένων στο ίδιο διάγραμμα διασποράς, παρέχοντας μια ολοκληρωμένη προβολή των δεδομένων σας.

#### Ε3. Μπορώ να προσαρμόσω την εμφάνιση του διαγράμματος διασποράς;
Ναι, χρησιμοποιώντας το Aspose.Words για .NET, μπορείτε να προσαρμόσετε διάφορες πτυχές της εμφάνισης του διαγράμματος διασποράς. Μπορείτε να τροποποιήσετε ιδιότητες όπως το χρώμα σειράς, το σχήμα δείκτη, τις ετικέτες αξόνων και τη μορφοποίηση της περιοχής γραφήματος. Η βιβλιοθήκη παρέχει ένα πλούσιο σύνολο API για τον έλεγχο των οπτικών στοιχείων του γραφήματος και τη δημιουργία μιας προσαρμοσμένης εμφάνισης που ταιριάζει στις ανάγκες σας.

#### Q4. Μπορώ να αποθηκεύσω το έγγραφο με το εισαγόμενο διάγραμμα διασποράς σε διαφορετικές μορφές;
Ναι, το Aspose.Words για .NET σάς επιτρέπει να αποθηκεύετε το έγγραφο με το εισαγόμενο διάγραμμα διασποράς σε διάφορες μορφές, όπως DOCX, PDF, HTML και άλλα. Μπορείτε να επιλέξετε την επιθυμητή μορφή εξόδου με βάση τις απαιτήσεις σας και να χρησιμοποιήσετε το`Save` μέθοδος του`Document` αντικείμενο αποθήκευσης του εγγράφου. Το εισαγόμενο διάγραμμα διασποράς θα διατηρηθεί στο αποθηκευμένο έγγραφο.

#### Q5. Μπορώ να τροποποιήσω τα δεδομένα και την εμφάνιση του διαγράμματος διασποράς μετά την εισαγωγή του;
Ναι, μετά την εισαγωγή του γραφήματος διασποράς στο έγγραφο, μπορείτε να τροποποιήσετε τα δεδομένα και την εμφάνισή του χρησιμοποιώντας τα API που παρέχονται από το Aspose.Words για .NET. Μπορείτε να ενημερώσετε τα δεδομένα της σειράς με νέες συντεταγμένες X και Y, να αλλάξετε τα σχήματα και τα χρώματα των δεικτών, να προσαρμόσετε τις ιδιότητες αξόνων και να εφαρμόσετε επιλογές μορφοποίησης για να δημιουργήσετε δυναμικά και διαδραστικά γραφήματα στα έγγραφα του Word.