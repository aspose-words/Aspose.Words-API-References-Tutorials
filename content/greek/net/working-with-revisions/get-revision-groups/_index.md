---
title: Λήψη ομάδων αναθεωρήσεων
linktitle: Λήψη ομάδων αναθεωρήσεων
second_title: Aspose.Words Document Processing API
description: Λάβετε ομάδες αναθεωρήσεων σε ένα έγγραφο του Word με το Aspose.Words για .NET.
type: docs
weight: 10
url: /el/net/working-with-revisions/get-revision-groups/
---

Σε αυτόν τον οδηγό βήμα προς βήμα, θα σας πούμε πώς να αποκτήσετε τις ομάδες αναθεώρησης σε ένα έγγραφο του Word χρησιμοποιώντας το Aspose.Words για .NET. Θα σας παρέχουμε τον πλήρη πηγαίο κώδικα και θα σας δείξουμε πώς να μορφοποιήσετε την έξοδο σήμανσης.

## Βήμα 1: Φόρτωση του εγγράφου

Το πρώτο βήμα είναι να ανεβάσετε το έγγραφο που περιέχει τις αναθεωρήσεις.

```csharp
Document doc = new Document(MyDir + "Revisions.docx");
```

## Βήμα 2: Περιήγηση σε ομάδες αναθεωρήσεων

Στη συνέχεια, θα περιηγηθούμε στις ομάδες αναθεωρήσεων που υπάρχουν στο έγγραφο και θα εμφανίσουμε τα στοιχεία τους, όπως ο συγγραφέας, ο τύπος αναθεώρησης και το αναθεωρημένο κείμενο.

```csharp
foreach(RevisionGroup group in doc.Revisions.Groups)
{
     Console.WriteLine("{0}, {1}:", group.Author, group.RevisionType);
     Console.WriteLine(group.Text);
}
```


### Παράδειγμα πηγαίου κώδικα για Λήψη ομάδων αναθεωρήσεων χρησιμοποιώντας Aspose.Words για .NET

Ακολουθεί ο πλήρης πηγαίος κώδικας για τη λήψη των ομάδων αναθεωρήσεων σε ένα έγγραφο χρησιμοποιώντας το Aspose.Words για .NET:

```csharp
Document doc = new Document(MyDir + "Revisions.docx");

foreach(RevisionGroup group in doc.Revisions.Groups)
{
	 Console.WriteLine("{0}, {1}:", group.Author, group.RevisionType);
	 Console.WriteLine(group.Text);
}
```

## συμπέρασμα

Σε αυτό το σεμινάριο, μάθαμε πώς να λαμβάνουμε τις ομάδες αναθεώρησης σε ένα έγγραφο του Word χρησιμοποιώντας το Aspose.Words για .NET. Ακολουθήσαμε τα βήματα για να φορτώσουμε το έγγραφο και να περιηγηθούμε στις ομάδες κριτικών, εμφανίζοντας λεπτομέρειες όπως ο συγγραφέας και ο τύπος κριτικής. Τώρα μπορείτε να εφαρμόσετε αυτήν τη γνώση για να αναλύσετε αναθεωρήσεις του δικού σας εγγράφου Word χρησιμοποιώντας το Aspose.Words για .NET.

### Συχνές ερωτήσεις

#### Ε: Πώς να ανεβάσετε ένα έγγραφο στο Aspose.Words για .NET;

 Α: Χρησιμοποιήστε το`Document` κλάση Aspose.Words για .NET για τη φόρτωση ενός εγγράφου από ένα αρχείο. Μπορείτε να καθορίσετε την πλήρη διαδρομή του εγγράφου.

```csharp
Document doc = new Document("path/to/the/document.docx");
```

#### Ε: Πώς να περιηγηθείτε σε ομάδες αναθεωρήσεων σε ένα έγγραφο στο Aspose.Words για .NET;

 Α: Χρησιμοποιήστε το`Groups` ιδιοκτησία του εγγράφου`Revisions` αντικείμενο για να λάβετε τη συλλογή των ομάδων αναθεώρησης. Στη συνέχεια, μπορείτε να χρησιμοποιήσετε έναν βρόχο για να πραγματοποιήσετε αναζήτηση σε κάθε ομάδα αξιολόγησης.

```csharp
foreach(RevisionGroup group in doc.Revisions.Groups)
{
     // Επεξεργαστείτε κάθε ομάδα κριτικής εδώ
}
```

#### Ε: Πώς να βρείτε τον συγγραφέα μιας ομάδας κριτικής στο Aspose.Words για .NET;

 Α: Χρησιμοποιήστε το`Author` ιδιοκτησία του`RevisionGroup` αντικείμενο να πάρει ο συγγραφέας της ομάδας αναθεώρησης.

```csharp
string author = group.Author;
```

#### Ε: Πώς να αποκτήσετε τον τύπο αναθεώρησης μιας ομάδας αναθεώρησης στο Aspose.Words για .NET;

 Α: Χρησιμοποιήστε το`RevisionType` ιδιοκτησία του`RevisionGroup`αντικείμενο για να λάβετε τον τύπο αναθεώρησης της ομάδας.

```csharp
string revisionType = group.RevisionType;
```